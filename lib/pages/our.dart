import 'package:flutter/material.dart';
class PaginaOur extends StatelessWidget {
  const PaginaOur({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Our", style: TextStyle(fontSize: 30),),
    );
  }
}